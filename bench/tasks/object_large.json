{
  "schema": {
    "type": "object",
    "properties": {
      "summary": {"type": "string"},
      "bullets": {"type": "array"},
      "priority": {"type": "string"}
    },
    "required": ["summary", "bullets", "priority"]
  },
  "prompts": [
    "Produce a JSON summary with 5 bullets about urban mobility improvements.",
    "Produce a JSON summary with 5 bullets about local food programs."
  ]
}